

// the variable likeCount would be the number of likes from a social media interaction.
var likeCount = 0;
// the function of the below code block would be to increase the number of likes. 
function increaseLikes() {
    // this is a step progression, where for every like the variable is increased by one. 
    likeCount = likeCount + 1;
}
